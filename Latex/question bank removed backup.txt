Removed from `question bank revised.tex` on 2026-07-24.

Q30. Which register tracks the top of the stack?
- PC
- SP
- IR
- ALU

Q32. Which event normally pushes data onto the stack?
- Memory read
- Function call
- Pipeline stall
- Cache miss

Q33. Why do CPUs use pipelining?
- To reduce instruction memory
- To increase instruction throughput
- To eliminate branching
- To reduce stack usage

Q38. If a branch prediction is incorrect:
- The CPU must flush incorrect instructions in the pipeline
- The stack pointer resets
- The instruction register deletes memory
- The ALU halts

Q39. Why are caches used in modern CPUs?
- To increase instruction size
- To store program code permanently
- To reduce average memory access time
- To remove the need for RAM

Q40. Which memory type is fastest?
- Hard disk
- RAM
- Cache
- Virtual memory

Q41. Which memory component is typically largest but slowest?
- Cache
- Registers
- Main memory
- Secondary storage

Q45. Consider this sequence: `lw $t0, 0($s0)` followed by `add $t1, $t0, $t2`. Why might a pipeline stall occur?
- Because the second instruction depends on `$t0`
- Because `$s0` is unused
- Because the program counter resets
- Because memory is cleared

Q46. Which register usually holds the return address after a function call in MIPS?
- `$zero`
- `$ra`
- `$sp`
- `$gp`

Q50. What is this instruction commonly used for? `addi $sp, $sp, -16`
- Allocate space on the stack
- Reset stack pointer
- Jump to a function
- Clear registers

Q51. What is this instruction commonly used for? `addi $sp, $sp, 16`
- Allocates stack memory
- Frees stack memory after a function returns
- Loads memory
- Changes the program counter

Q53. What does this instruction do? `jal func`
- Calls a function and stores the return address in `$ra`
- Jumps without saving the return address
- Loads memory
- Writes to the stack
